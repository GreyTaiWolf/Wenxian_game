# 地图内容与事件文档

本文档记录两层格子地图、世界 POI、local map、城市/地点信息和后续地图事件扩展规则。新增地图事件、采集点、城市内容、剧情点或地点区域时，优先同步本文档，再检查 `docs/GAME_DESIGN.md` 与 `docs/BALANCE.md`。

## 地图层级

当前历练地图分为两层：

- 大世界：地图 ID 为 `world`，尺寸 `320x200`，大世界每格代表 `100` 里。大世界直接显示青云镇、黑风山、灵药谷、天玄城、青云宗、万兽山、洞府、秘境、禁地、资源点等 POI，并使用 `regionTag` / `climateTag` 表达中州、南疆、东海、西漠、北境等世界区域。
- local map：地图 ID 统一为 `local:<locationId>`，用于城市、城镇、山脉、宗门、秘境、洞府等内部探索；玩家可见 UI 统一显示为“内部地图”。城市每格约 `35` 米，城镇每格约 `50` 米，秘境每格约 `1` 里，山脉与野外每格约 `2` 里。
- 场景图：进入具体地点后，地点内部的场景、建筑、剧情图片继续保留；青云镇、黑风山、小小仙铺、李百草草药铺等图片场景不作为大世界或 local map 主背景。
- 地图头部：大世界和 local map 统一使用紧凑 HUD 标题栏。大世界显示“问仙大世界”和当前抵达/正在前往的 POI；local map 显示地点名与“当前：某场景”。普通玩家界面不常驻显示放大、缩小、重置、网格和自检按钮，缩放保留滚轮/触控手势；所有 local map 的标题栏右侧都显示人物入口，城市/城镇显示“城镇人物”，非城镇显示“此地人物”，人物、商店和任务通过居中弹窗进入。

中州、南疆、东海、西漠、北境不再作为可进入的中间地图层；`World_map.png`、`World_map_nanjiang2.png`、`World_map_zhonzhou2.png` 不再作为主要地图背景依赖。

## 坐标规则

- 大世界坐标使用零基准 `x,y` 格子坐标，例如青云镇 POI 在 `142,92`。
- local map 坐标同样使用零基准格子坐标，场景入口由 `src/data/gridMaps.ts` 根据地点场景生成；地点配置可用 `localSceneCoords` 覆盖单个场景坐标。
- 旧 `48x32`、`24x16` 图片地图坐标加载时会归一化到新大世界或对应 local map 默认坐标。
- `anchor` 是 POI 或场景 zone 的中心/入口参考格，不一定是玩家最终停留格。
- 普通模式下的大世界 POI 标记放在 `anchor` 附近；整片 zone 仍负责点击移动后的信息命中。
- 玩家点击不可走格时，先修正到最近可走格，再按最终停留格判断是否命中 zone。`wild` 与 `secret` 内部地图只允许沿显式路径和场景锚点移动，城市/城镇仍保留自由寻路。
- `eventIds` 和 `src/data/events.ts` 共同服务地图事件。当前版本事件由行走抵达后的通用事件引擎按地图、区域标签、天气、冷却和概率判定，不要求每个 zone 都手写事件 ID。

## Zone 尺寸规则

来源：`src/data/gridMapZones.ts`

| 类型 | 格数 | 形状 | 用途 |
| --- | ---: | --- | --- |
| 大世界 POI | 1-25 | `1x1` 至 `5x5` | 城、镇、山脉、宗门、秘境、禁地、资源点预览与进入 |
| local map 场景入口 | 9 | `3x3` | 地点内部场景、建筑、NPC 热点、采集点或战斗区 |
| 小入口预留 | 2 | `1x2` | 后续洞口、传送阵、狭窄关隘 |

## 大世界 POI

来源：`src/data/worldPois.ts`

| poiId | 名称 | regionTag | 坐标 | 类型 | enterMapId |
| --- | --- | --- | --- | --- | --- |
| `qingyun_city` | 青云镇 | 中州 | `142,92` | 城镇 | `local:qingyun_city` |
| `tian_xuan_gate` | 天玄城门 | 中州 | `170,88` | 城市 | `local:tian_xuan_gate` |
| `black_wind_mountain` | 黑风山 | 中州 | `118,84` | 山脉 | `local:black_wind_mountain` |
| `herb_valley` | 灵药谷 | 中州 | `150,110` | 山谷 | `local:herb_valley` |
| `ancient_cave` | 古修洞府 | 中州 | `198,68` | 洞府 | `local:ancient_cave` |
| `qingyun_sect` | 青云宗 | 中州 | `150,72` | 宗门 | 预留 |
| `baicao_valley` | 百草谷 | 南疆 | `156,156` | 山谷 | `local:baicao_valley` |
| `ten_thousand_beast_mountain` | 万兽山 | 南疆 | `136,144` | 山脉 | `local:ten_thousand_beast_mountain` |
| `miasma_marsh` | 瘴雾沼泽 | 南疆 | `108,174` | 禁地/沼泽 | `local:miasma_marsh` |
| `central_spirit_mine` | 中州灵矿 | 中州 | `176,118` | 资源点 | 预留 |
| `southern_herb_spring` | 南疆药泉 | 南疆 | `168,168` | 资源点 | 预留 |

玩家自由点击并抵达大世界 POI zone 后，只自动展开地点预览卡。开放 POI 可进入对应 local map；未开放 POI 只显示保留说明、区域标签和后续内容提示。

## Local Map Zone

| zoneId 规则 | mapId | targetId | 覆盖格 | 用途 |
| --- | --- | --- | --- | --- |
| `local:<locationId>:scene:<sceneId>` | `local:<locationId>` | `<sceneId>` | 场景锚点周围 `3x3` | 到达后切换地点内部场景 |
| `world:poi:<poiId>` | `world` | `<poiId>` | 由 POI `zoneSize` 决定 | 到达后展开大世界地点预览 |

玩家自由点击并抵达 local map 场景 zone 后，会切换到对应地点内部场景；进入地点仍需要先在大世界 POI 预览卡中确认。local map 的区域、道路、不可通行格、灵气浓度和危险等级由 `src/data/gridMaps.ts` 生成，不新增奖励、任务或战斗组。`wild` 与 `secret` 地点会按 `localSceneLinks` 或默认链式路径生成可走格，玩家只能沿这些路径前往场景；城市/城镇继续允许在非阻挡格自由移动。大世界和 local map 不在地图内部重复渲染“内部地图 / 当前”标题块，统一由页面顶部 HUD 标题栏展示当前位置。所有内部地图的人物入口统一从当前地点 NPC 名册或对应地图标记打开居中弹窗，不在地图下方常驻列表卡片；城市/城镇使用“城镇人物”，非城镇使用“此地人物”。内部地图上的场景/地点标记只显示名称，不显示类型副标题；固定 NPC 不作为地图人物标记显示，而是在点击地点后由地点弹窗的“此地修士”列表承载。点击人物名册或内部地图游历 NPC 标记时，先按该 NPC 的 `homeSceneId`、店铺场景或默认落脚场景寻路；角色抵达后再打开人物对话。点击内部地图地点标记时同样先寻路，抵达后打开“在某地点”的地点弹窗，弹窗列出此地修士和设施；有管事 NPC 的商店/建筑优先由 NPC 打开交易、任务、打造、洗炼等功能，无 NPC 地点才直接显示设施按钮。

### 青云镇内部场景

青云镇进入地点后使用 `local:qingyun_city` 格子 local map。玩家在地图上移动到场景入口后切换当前场景；地图顶部使用统一 HUD 标题栏显示“青云镇”和当前场景，右侧保留“城镇人物”按钮，不显示缩放、重置、网格或自检工具。地图标记保持轻量，只显示城主府、小小仙铺、赵家炼器铺等地点名称；清雨、赵铁匠、李百草等固定 NPC 不直接铺在地图上，点击地点后在地点弹窗中显示。地点描述、场景列表、资源、事件、敌人、掉落和内联商店不在地图下方常驻展示，后续城镇内容以人物名册、场景动作和事件弹窗承载。

| 热点 | 场景 ID | 三级格 | 类型 | 说明 |
| --- | --- | --- | --- | --- |
| 小小仙铺 | `xiaoxiao_shop` | `20,8` | 交易 | 对应清雨和小小仙铺商店弹窗 |
| 赵家炼器铺 | `zhao_refinery` | `22,13` | 交易 / 炼器 | 抵达后打开“在赵家炼器铺”，赵铁匠为管事，可交易、打造装备和洗炼词条 |
| 陈半仙 | `chen_banxian_stall` | `17,12` | NPC 对话 | 摆摊卖秘籍的老头，当前为对话与功法系统预告 |
| 李百草 | `li_baicao_herbs` | `12,11` | 交易 | 对应李百草、苏达和草药铺商店弹窗 |
| 城主府 | `city_manor` | 预留 | NPC 对话 | 保留青云令牌入宗入口 |
| 公告栏 | `notice_board` | `26,5` | 任务 | 保留中州任务榜入口 |
| 青云客栈 | `qingyun_inn` | `30,11` | NPC 对话 | 客栈老板娘消息与顾青萝招募入口 |
| 灵田 | `inn_spirit_field` | `33,12` | NPC 对话 | 青云客栈老板娘的灵田，当前只展示观察反馈 |

### 小小仙铺第四级场景

小小仙铺当前通过青云镇“城镇人物”名册进入清雨 NPC 弹窗，也保留 `sceneNpcHotspots.qing_yu` 作为旧场景热点配置。清雨可聊天或打开小小仙铺商店；商店使用居中分类商品格弹窗，手机每页 8 格、宽屏每页 9 格。

| 场景 | 热点 ID | NPC | 画面位置 | 交互 |
| --- | --- | --- | --- | --- |
| 小小仙铺 | `qing_yu` | 清雨 | 旧场景热点 `63.5%,31%` | 城镇人物名册 / 旧热点均打开 NPC 对话弹窗 |

### 赵家炼器铺服务

赵家炼器铺当前通过青云镇 local map 地点标记进入地点弹窗，地点弹窗显示赵铁匠并标记为管事。赵铁匠提供聊天、交易、打造和洗炼；交易打开赵家炼器铺商店，商店售卖粗铁剑图纸和低阶法剑图纸。打造使用 `zhao_refinery_workshop` 配方，未学配方时必须先研读对应图纸；洗炼先选择背包装备或已穿装备，再进入居中洗炼台并按品级限制锁词条数量。

| 服务 | 入口 NPC | 数据 ID | 说明 |
| --- | --- | --- | --- |
| 交易 | 赵铁匠 | `zhao_refinery` | 沿用赵家炼器铺商店库存和季度刷新 |
| 图纸研读 | 赵铁匠 | `rough_iron_sword_blueprint / low_sword_blueprint` | 消耗图纸并永久解锁对应打造配方 |
| 打造 | 赵铁匠 | `forge_rough_iron_sword / forge_low_sword` | 已学配方后消耗灵石和炼器材料，产出独立装备实例，不消耗商店库存 |
| 洗炼 | 赵铁匠 | `reforgeEquipment` | 支持背包装备 / 已穿装备来源，保留装备主属性、阶位、品级和部位，只重掷未锁定副词条 |

### 李百草草药铺第四级场景

李百草草药铺当前通过青云镇“城镇人物”名册进入李百草或苏达 NPC 弹窗，也保留 `sceneNpcHotspots.su_da` 作为旧场景热点配置。购买会打开李百草草药铺商店弹窗，沿用通用分类商品格和分页规则。

| 场景 | 热点 ID | NPC | 画面位置 | 交互 |
| --- | --- | --- | --- | --- |
| 李百草 | `su_da` | 苏达 | 旧场景热点 `33.5%,47%` | 城镇人物名册 / 旧热点均打开 NPC 对话弹窗 |

### 黑风山内部场景

黑风山进入地点后使用 `local:black_wind_mountain` 格子 local map，默认入口场景为山道。黑风山属于 `wild` 地点，玩家只能沿显式路径前往场景；点击非路径格时会修正到最近可走路径格。当前开放山道、黑灵泉、山狼巢穴、墨金洞、黑风营地和黑风妖寨，敌人池为 `wolf_pack / black_wind_duo`，掉落池以妖兽骨、灵草和灵石为主，任务入口由青云镇公告栏的 `hunt_black_wind` 串联。

| 热点 | 场景 ID | local 格 | 类型 | 说明 |
| --- | --- | --- | --- | --- |
| 山道 | `mountain_path` | `28,7` | 野外 | 黑风山入口观察点，位于右上附近但不贴边，提示山中伏击风险 |
| 黑灵泉 | `black_spirit_spring` | `20,9` | 灵泉 | 黑雾泉眼，当前提供探查反馈 |
| 山狼巢穴 | `wolves` | `26,14` | 妖兽战斗 | 狼群巢穴，沿用 `wolf_pack` |
| 墨金洞 | `mojin_cave` | `19,17` | 洞窟战斗 | 洞中妖狼，沿用 `wolf_pack` |
| 黑风营地 | `cultivators` | `32,12` | 修士战斗 | 妖修营地，沿用 `black_wind_duo` |
| 黑风妖寨 | `black_wind_demon_stockade` | `34,16` | 妖修战斗 | 妖修营寨，沿用 `black_wind_duo` |

| 路径 | 说明 |
| --- | --- |
| 山道 - 黑灵泉 | 山道向山腹灵泉分支 |
| 山道 - 山狼巢穴 | 山道向妖兽巢穴分支 |
| 山道 - 黑风营地 | 山道向妖修活动区分支 |
| 黑灵泉 - 墨金洞 | 灵泉下方石阶通往矿洞 |
| 山狼巢穴 - 墨金洞 | 狼群巢穴与洞口相连 |
| 黑风营地 - 黑风妖寨 | 妖修营地继续通往妖寨 |

### 第一章地点内容池

| 地点 | 图片资源 | 随机事件池 | 敌人池 | 掉落池 | NPC / 任务入口 |
| --- | --- | --- | --- | --- | --- |
| 青云镇 | `qingyun_town`，小小仙铺 `xiaoxiao_shop`，李百草 `li_baicao_herbs` | `qingyun_notice_tip`、`mist_traveling_cultivator` | 安全区，无常规敌人 | 灵草、灵草种、少量灵石事件 | 清雨、苏达、赵铁匠、陈半仙、李百草、罗执事、云娘、顾青萝、过路修士；公告栏任务 `collect_qi_grass / hunt_black_wind / deliver_letter` |
| 黑风山 | `black_wind_mountain` | `wild_beast_ambush`、`black_wind_bone_cache` | `wolf_pack`、`black_wind_duo` | 妖兽骨、灵草、灵石、黑风任务材料 | 通过青云镇公告栏和客栈传闻接入 `hunt_black_wind` |

### 中州场景热点

| 地点 | 场景 | 热点 ID | NPC | 画面位置 | 交互 |
| --- | --- | --- | --- | --- | --- |
| 天玄城门 | 天玄城门 | `shen_guanlan` | 沈观澜 | 左侧登记修士，`22%,78%` | 点击姓名弹出城门登记对话 |
| 天玄城门 | 天玄城门 | `lu_xuanheng` | 陆玄衡 | 右侧守门修士，`76%,76%` | 点击姓名弹出守门规矩对话 |
| 青云镇 | 城镇人物 | `qing_yu` | 清雨 | 小小仙铺 | 打开 NPC 对话，可进入小小仙铺商店 |
| 青云镇 | 城镇人物 | `su_da` | 苏达 | 李百草草药铺 | 打开 NPC 对话，可进入李百草草药铺 |
| 青云镇 | 城镇人物 | `zhao_tiejiang` | 赵铁匠 | 赵家炼器铺 | 打开 NPC 对话，可进入炼器铺商店、打造和洗炼 |
| 青云镇 | 城镇人物 | `city_manor_clerk` | 罗执事 | 城主府 | 打开 NPC 对话，可进入任务榜 |

### 场景资源与热点组件规则

- 图片资源统一从 `src/data/assets.ts` 读取，场景图、建筑图、剧情图、NPC 头像和物品图标都先登记在资源入口，业务组件不再散落硬编码路径；大世界与 local map 的主要地图背景由 CSS 和格子数据生成。
- 现有真实图片继续用 Vite `new URL(...)` 引入；未来内容可先登记 `/assets/...` 规划路径，但组件必须允许图片加载失败。
- 场景图缺失时，`SceneView` 显示场景名、类型和短描述，不阻塞行动按钮、NPC 热点或文字反馈。
- NPC 头像缺失时，NPC 对话弹窗显示默认头像占位；物品图标缺失时继续使用 `GameIcon` 语义图标。
- 城镇 NPC 名册、关系网、游历路线和互动按钮集中登记在 `src/data/npcs.ts`；旧场景图热点仍可保留在 `src/data/world.ts` 的 `sceneNpcHotspots` 作为兼容配置。
- 热点坐标使用百分比 `x/y`，左上角为 `0%,0%`，右下角为 `100%,100%`；按钮点击区域不小于 42px。
- 热点类型预留 `npc / shop / portal / action`；NPC、商店和任务交互统一接入居中弹窗，不改变地图寻路、奖励或任务触发。

## 南疆 Local Map 内容参考

| 地点 | 大世界 poiId | regionTag | local map | 说明 |
| --- | --- | --- | --- | --- |
| 巫妖盟 | `wuyao_alliance` | 南疆 | `local:wuyao_alliance` | 南疆盟城与坊市 |
| 木灵宗 | `wood_spirit_sect` | 南疆 | `local:wood_spirit_sect` | 草木宗门与试炼入口 |
| 百草谷 | `baicao_valley` | 南疆 | `local:baicao_valley` | 灵药采集与妖藤战斗 |
| 万兽山 | `ten_thousand_beast_mountain` | 南疆 | `local:ten_thousand_beast_mountain` | 妖兽战斗与灵宠踪迹 |
| 瘴雾沼泽 | `miasma_marsh` | 南疆 | `local:miasma_marsh` | 瘴气危险区 |
| 千瀑灵崖 | `thousand_falls_cliff` | 南疆 | `local:thousand_falls_cliff` | 秘境与灵瀑守卫 |
| 潮汐海市 | `tide_market` | 南疆 | `local:tide_market` | 海边坊市 |
| 归潮礁岛 | `returning_tide_reef` | 南疆 | `local:returning_tide_reef` | 潮音秘洞与宝匣 |

玩家在大世界抵达这些 POI 后展开地点预览卡；确认后进入对应 local map。旧南疆图片地图坐标只作为历史内容参考，不再作为可进入中间层。

## 后续事件规则

新增地图事件时建议使用以下流程：

1. 先在本文件记录事件名称、关联 zone、触发条件、显示文案和奖励/战斗预期。
2. 若事件影响奖励、掉落、战斗或概率，同步更新 `docs/BALANCE.md`。
3. 若事件影响世界内容、任务线或系统关系，同步更新 `docs/GAME_DESIGN.md`。
4. 再在 `src/data` 中添加正式配置，并确保 ID 使用英文 `lower_snake_case`。
5. 保持 zone 命中只负责“发现与弹出信息”，不要绕过地点确认直接触发收益。

## 2026-05-30：场景图退场与清晰地图标注

- 场景展示：地点内部不再展示 `sceneMapImageKey` 或 `imageKey` 对应的大幅场景图，统一使用 local map、场景按钮、行动按钮和 NPC 热点列表承载探索。
- 资源入口：`src/data/assets.ts` 的 `sceneImages` 保留空表兼容旧字段，但不再通过 `new URL(...)` 静态引入场景图，避免构建继续打包青云镇、黑风山、仙铺等场景大图。
- 地图文字：大世界和 local map 的 POI 名称、区域标签、玩家位置标记改为视口覆盖层定位，不再作为被缩放画布的一部分渲染，从视觉上修复高倍缩放字体模糊。
- 网格显示：普通缩放状态不再显示地形格色块，路线格、道路线、POI 和当前位置仍显示；网格、阻挡、zone 和路径细节只在开发调试层启用，不作为普通玩家常驻按钮展示。
- 规则边界：本次只改变展示和资源依赖，不改 `gridMaps`、`gridMapZones`、`roadSegments`、A* 道路优先、移动耗时、任务奖励或事件触发规则。

## 已接入地图事件（2026-05-23）

| 事件 ID | 类型 | 触发范围 | 条件 | 结果 |
| --- | --- | --- | --- | --- |
| `mist_traveling_cultivator` | 对话 / 灵田 | 中州、南疆行走 | 7% 概率，20 天冷却 | 可交谈获得灵草种。 |
| `qingyun_notice_tip` | 任务 / 奖励 | 青云镇 | 10% 概率，12 天冷却 | 阅读旧悬赏，获得黑风山线索和少量灵石。 |
| `wild_beast_ambush` | 战斗 | 黑风山 | 5.5% 概率，18 天冷却 | 可选择狼群战斗或避开。 |
| `black_wind_bone_cache` | 机缘 / 战斗 | 黑风山 | 12% 概率，10 天冷却 | 可收起妖兽骨，或追踪脚印触发狼群战斗。 |
| `spirit_rain_seedfall` | 灵田 / 对话 | 中州、南疆行走 | 灵雨或灵潮天气，18% 概率，45 天冷却 | 可获得凝气草种与灵泉水。 |
| `starfall_omen` | 天象 / 机缘 | 全地图行走 | 2.5% 概率，120 天冷却，最多 3 次 | 可触发星陨天气并获得五色息壤。 |

事件触发后会打开底部事件抽屉，由玩家选择交谈、战斗、采集、观测或离开。战斗事件接入现有战斗组，不在地图命中瞬间直接结算奖励。
## 2026-05-28：世界道路网络与 12x 微观缩放

- 大世界和 local map 的格子地图最大缩放为 `12x`；图片场景地图不跟随本次扩大，仍保持原有场景图交互规则。
- 大世界道路网络由 `src/data/gridMaps.ts` 的 `worldRoadPairs` 与 `roadSegments` 维护，所有 `src/data/worldPois.ts` 中的 POI 至少接入一条道路。新增世界 POI 时必须同时补一条道路连接，避免出现孤立点。
- 当前主干覆盖：青云镇连接青云宗、黑风山、天玄城、灵药谷、落霞镇；天玄城连接古洞府和中州灵矿；落霞镇连接西漠禁地；灵药谷连接百草谷；巫妖盟连接木灵宗、百草谷、南疆药泉和潮汐海市；百草谷连接万兽山、瘴雾沼泽、千瀑灵崖和南疆药泉；潮汐海市连接东海航线与归潮礁岛；古洞府连接北境雪原，北境雪原回接青云宗。
- 道路表现规则：缩小时用淡金色 SVG 线条显示区域主干路；高倍缩放进入微观格子层后，具体道路格会继续以道路地形底色显示，路线格、玩家标记、目标高亮叠加在其上。
- 寻路规则：A* 独立使用道路优先代价，倾向小幅绕路走已配置道路；移动耗时仍按地图 `cellDistance` 与目标格 `movementCost` 计算，不把道路偏好直接变成移动时间折扣。
