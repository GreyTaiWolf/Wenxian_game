import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.greytaiwolf.wenxian",
  appName: "问仙",
  webDir: "dist",
  android: {
    backgroundColor: "#0b111b",
  },
};

export default config;
